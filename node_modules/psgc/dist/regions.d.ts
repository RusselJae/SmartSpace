import { MappedRegion } from "./PsgcInterface";
declare const _default: {
    all: () => Array<MappedRegion>;
    find: (name: string) => MappedRegion | void;
    filter: (name: string) => Array<MappedRegion>;
};
export default _default;
