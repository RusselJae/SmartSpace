"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const provinces_json_1 = __importDefault(require("./data/provinces.json"));
const municipalities_json_1 = __importDefault(require("./data/municipalities.json"));
const findProvinceMunicipalities = (provinceName) => {
    return municipalities_json_1.default.filter((municipality) => {
        return municipality.province === provinceName;
    });
};
exports.default = {
    all: () => {
        return provinces_json_1.default.map((province) => {
            const provinceMunicipalities = findProvinceMunicipalities(province.name);
            return Object.assign(Object.assign({}, province), { municipalities: provinceMunicipalities });
        });
    },
    find: (name) => {
        const province = provinces_json_1.default.find((province) => province.name === name);
        if (!province)
            return undefined;
        const provinceMunicipalities = findProvinceMunicipalities(province.name);
        return Object.assign(Object.assign({}, province), { municipalities: provinceMunicipalities });
    },
    filter: (name) => {
        const searchPattern = new RegExp(`[a-zA-Z\\s]*${name}[a-zA-Z]*`, "i");
        const filteredProvinces = provinces_json_1.default.filter((province) => !!searchPattern.exec(province.name));
        return filteredProvinces.map((province) => {
            const provinceMunicipalities = findProvinceMunicipalities(province.name);
            return Object.assign(Object.assign({}, province), { municipalities: provinceMunicipalities });
        });
    },
};
