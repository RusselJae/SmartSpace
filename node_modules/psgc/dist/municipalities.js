"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const municipalities_json_1 = __importDefault(require("./data/municipalities.json"));
const barangays_json_1 = __importDefault(require("./data/barangays.json"));
const findMunicipalityBarangays = (municipalityName) => {
    return barangays_json_1.default.filter((barangay) => {
        return barangay.citymun === municipalityName;
    });
};
exports.default = {
    all: () => {
        return municipalities_json_1.default.map((municipality) => {
            const municipalityBarangays = findMunicipalityBarangays(municipality.name);
            return Object.assign(Object.assign({}, municipality), { barangays: municipalityBarangays });
        });
    },
    find: (name) => {
        const municipality = municipalities_json_1.default.find((municipality) => municipality.name === name);
        if (!municipality)
            return undefined;
        const municipalityBarangays = findMunicipalityBarangays(municipality.name);
        return Object.assign(Object.assign({}, municipality), { barangays: municipalityBarangays });
    },
    filter: (name) => {
        const searchPattern = new RegExp(`[a-zA-Z\\s]*${name}[a-zA-Z]*`, "i");
        const filteredMunicipalities = municipalities_json_1.default.filter((municipality) => !!searchPattern.exec(municipality.name));
        return filteredMunicipalities.map((municipality) => {
            const municipalityBarangays = findMunicipalityBarangays(municipality.name);
            return Object.assign(Object.assign({}, municipality), { barangays: municipalityBarangays });
        });
    },
};
