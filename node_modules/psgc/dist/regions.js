"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const regions_json_1 = __importDefault(require("./data/regions.json"));
const provinces_json_1 = __importDefault(require("./data/provinces.json"));
const findRegionProvinces = (regionDesignation) => {
    return provinces_json_1.default.filter((province) => {
        return province.region === regionDesignation;
    });
};
exports.default = {
    all: () => {
        return regions_json_1.default.map((region) => {
            const regionProvinces = findRegionProvinces(region.designation);
            return Object.assign(Object.assign({}, region), { provinces: regionProvinces });
        });
    },
    find: (name) => {
        const region = regions_json_1.default.find((region) => region.name === name);
        if (!region)
            return undefined;
        const regionProvinces = findRegionProvinces(region.designation);
        return Object.assign(Object.assign({}, region), { provinces: regionProvinces });
    },
    filter: (name) => {
        const searchPattern = new RegExp(`[a-zA-Z\\s]*${name}[a-zA-Z]*`, "i");
        const filteredRegions = regions_json_1.default.filter((region) => !!searchPattern.exec(region.name));
        return filteredRegions.map((region) => {
            const regionProvinces = findRegionProvinces(region.designation);
            return Object.assign(Object.assign({}, region), { provinces: regionProvinces });
        });
    },
};
